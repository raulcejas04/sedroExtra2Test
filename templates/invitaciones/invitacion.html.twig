{% extends 'base.html.twig' %}

{% block title %}
  Nueva Invitación
{% endblock %}

{% block body %}
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <h1>
          Nueva Invitación
        </h1>
      </div>
    </div>
    <div class="row">
      <div class="col-md-6">
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" value="Crear" class="btn btn-success" />
        {{ form_rest(form) }}
        {{ form_end(form) }}
      </div>
    </div>
  </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
 {#    {{ encore_entry_script_tags('invitacion') }} #}
{% endblock %}
