  {{form_start(formReenviarCorreo,{'action':path('invitacion_reenviarEmail',{'hash':invitacion.hash})})}}
                    <div class="modal fade" id="reenviarEmailModal" tabindex="-1" aria-labelledby="reenviarEmailModalLabel" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="exampleModalLabel">Reenviar Email</h5>
                           {#  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button> #}
                        </div>
                        <div class="modal-body">
                                {{form_label(formReenviarCorreo.email)}}
                                {{form_widget(formReenviarCorreo.email)}}
                                {{form_errors(formReenviarCorreo.email)}}
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="modal-close btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                            <button type="submit" class="modal-submit btn btn-primary">Aceptar</button>
                        </div>
                        </div>
                    </div>
                    </div>
{{form_end(formReenviarCorreo)}}